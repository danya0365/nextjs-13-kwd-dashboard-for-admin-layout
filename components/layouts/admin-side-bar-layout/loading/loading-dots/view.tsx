import s from './styles.module.css';

const LoadingDotsView = () => {
  return (
    <span className={s.root}>
      <span />
      <span />
      <span />
    </span>
  );
};

export default LoadingDotsView;
